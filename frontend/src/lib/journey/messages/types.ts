export type JourneyMessageConfig = {
  /** Optional heading displayed above the message body. */
  heading?: string;
  /** Primary message shown to the learner. */
  message: string;
  /** Custom label for the call-to-action button. */
  ctaLabel?: string;
};
